# session
session:

    store:

        module: session-file-store
        options:
            path: "%kernel.var_path%/sessions"
            ttl: 7200
            retries: 4
            fileExtension: .session

# webpack
webpack:

    # the path to the config file
    config.path: "demo-bundle:resources/public/webpack.prod.js"
