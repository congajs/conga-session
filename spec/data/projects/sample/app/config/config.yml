# include other config files
imports:
    - { resource : parameters.ini }
    - { resource : bundles.yml }

framework:

    # application settings
    app:
        host: "%app.http.host%"
        port: "%app.http.port%"

# validation
session:

    name: super.awesome.cookie

    cookie:

    # NOTE: session store is configured in environment specific configuration file
