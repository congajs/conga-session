const package = require('../../../package.json');

module.exports = {

    id: 'session',
    name: 'Session',
    description: 'Session',
    bundle: package.name,
    version: package.version,
    icon: null

};
