# sessions
session:

    secret: 'keyboard cat'
    resave: true
    saveUninitialized: true

    mixins:
        enabled: true
        #disabled:
        #    - flashBag
        #    - iterable

    cookie:

        name: conga.sid

    store:
