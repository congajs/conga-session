# sessions
session:

    secret: 'keyboard cat'
    resave: true
    saveUninitialized: true

    mixins:
        enabled: true
        #disabled:
        #    - flashBag
        #    - iterable


    name: conga.sid

    profiler:
        enabled: true

    store:
